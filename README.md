# Vibhor's coverletter

## Build

To build the PDF from the LaTeX source, run the following in the devcontainer:

```bash
pdflatex -shell-escape vibhor_coverletter.tex

# or using the alias command:
gen_tex_doc -- <tex_file>
```
